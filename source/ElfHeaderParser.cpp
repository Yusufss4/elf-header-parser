
#include <greeter/elf_header_parser.h>

using namespace elf_header;

class CommandLineParser {
    // Parse command line arguments
    // Determine mode of operation
};

class ElfFileLoader {
    // Load ELF file
    // Read necessary data from ELF file
};

class ElfHeaderParser {
    // Parse ELF file header
    // Determine endianness
    // Store parsed information in ElfFileHeader
};

class ElfFileHeader {
    // Store parsed information about ELF file header
};

class EndianConverter {
    // Convert endianness of data
};
